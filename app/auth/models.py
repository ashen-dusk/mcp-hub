# No custom models needed - using Django's built-in User model
# We'll store Google OAuth info in User model fields:
# - email (already exists)
# - first_name (already exists) 
# - last_name (already exists)
# - Additional fields can be added to User model if needed
